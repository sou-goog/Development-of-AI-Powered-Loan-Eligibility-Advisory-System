services:
  - type: web
    name: ai-loan-backend
    runtime: python
    plan: free  # Free tier
    pythonVersion: "3.11"  # SQLAlchemy 2.0.23 not compatible with Python 3.13
    buildCommand: cd backend && pip install -r requirements.txt
    startCommand: cd backend && python main.py
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ai-loan-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: GROQ_API_KEY
        sync: false  # Set in Render dashboard
      - key: DEEPGRAM_API_KEY
        sync: false  # Set in Render dashboard

databases:
  - name: ai-loan-db
    databaseName: ai_loan_system
    user: ai_loan_user
    plan: free